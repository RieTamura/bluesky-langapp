# Server Configuration
PORT=3000
NODE_ENV=development

# Bluesky API Configuration
BLUESKY_SERVICE_URL=https://bsky.social

# Database Configuration (for future use)
DATABASE_URL=sqlite:./data/database.sqlite

# Session Secret for express-session
SESSION_SECRET=your-super-secret-session-key-here

# Frontend URL for CORS
FRONTEND_URL=http://localhost:4321

# JWT Secret (for future authentication)
JWT_SECRET=your-super-secret-jwt-key-here

# Dictionary API Configuration (for future use)
DICTIONARY_API_KEY=your-dictionary-api-key-here

# Optional bind address configuration for the server
# BIND_ADDRESS - optional IP or hostname to bind the HTTP server to.
#   - Default: 127.0.0.1 (localhost) for safety.
#   - If set to 0.0.0.0 the server will bind to all interfaces (LAN). This is
#     considered unsafe in production unless you explicitly allow it by setting
#     BIND_ALL_OVERRIDE=true.
# HOST is treated as an alias for BIND_ADDRESS.
# BIND_ADDRESS=127.0.0.1
# BIND_ADDRESS=0.0.0.0
# BIND_ALL_OVERRIDE=false