# Force npm to use legacy peer dependency resolution
# This is required for React 19 compatibility with some packages
legacy-peer-deps=true

# Disable fund messages during install
fund=false

# Disable audit during install (speeds up builds)
audit=false
